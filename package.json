{
  "name": "klayde",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "windows": "bun run index.ts -- --os=windows",
    "macos": "bun run index.ts -- --os=macos",
    "linux": "bun run index.ts -- --os=linux",
    "all": "bun run index.ts -- --os=windows,macos,linux"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^7"
  }
}
